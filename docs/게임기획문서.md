# 게임 기획서 — StoneRush

## 1. 게임 개요

| 항목 | 내용 |
|---|---|
| 게임명 | StoneRush |
| 장르 | 2D 생존 회피 게임 |
| 플랫폼 | Windows PC |
| 개발 환경 | C++, glc2d_lib, Visual Studio 2022 |
| 개발 인원 | 1명 |
| 개발 기간 | 4주 |
| 게임 목적 | 광산에서 떨어지는 돌을 피하며 최대한 오래 생존하고 높은 점수를 획득한다. |

플레이어는 광산에 있는 광부를 조작한다. 광부는 화면 아래에서 좌우로 이동하며 위에서 떨어지는 돌을 피한다.

생존 시간이 길어질수록 점수가 증가하고, 돌에 한 번이라도 닿으면 게임이 종료된다. 결과 화면에서 최종 점수를 확인하고 다시 도전할 수 있다.

---

## 2. 핵심 플레이 방식

플레이어는 돌이 떨어지는 위치를 확인하고 안전한 위치로 이동한다.

```text
돌 생성
→ 낙하 위치 확인
→ 광부를 이동하여 회피
→ 생존 시간에 따라 점수 증가
→ 다음 돌을 계속 회피
```

돌과 충돌할 때까지 위 과정을 반복한다. 떨어지는 돌을 보며 이동 방향과 타이밍을 판단하는 것이 핵심이다.

---

## 3. 조작 방법

| 입력 | 기능 |
|---|---|
| A / D | 캐릭터 좌우 이동 |
| W / S | UI 선택 항목 위아래 이동 |
| Space | 상호작용 — 선택한 UI 항목 실행 |

- A/D로 광부를 각각 왼쪽과 오른쪽으로 이동하며, 두 키를 동시에 입력하면 정지한다.
- 타이틀과 결과 화면에서는 W/S로 항목을 선택한다.
- Space 키로 선택한 UI 항목을 실행한다.

---

## 4. 기본 게임 규칙

| 항목 | 규칙 |
|---|---|
| 플레이 인원 | 1명 |
| 플레이 공간 | 광산 배경의 고정된 화면 |
| 이동 범위 | 화면 아래쪽 땅 위의 좌우 영역 |
| 목숨 | 1회 |
| 광부와 돌의 충돌 | 즉시 게임 종료 |
| 돌과 땅의 충돌 | 해당 돌 제거 |
| 제한 시간 | 없음 |
| 진행 방식 | 광부가 돌과 충돌할 때까지 생존 |
| 결과 기준 | 종료 시 획득한 최종 점수 |

광부는 땅 위의 일정한 높이에서 좌우로 이동하며, 화면 밖으로 나갈 수 없다. 화면의 왼쪽 또는 오른쪽 끝에 도달하면 해당 방향으로 더 이동하지 않는다.

별도의 스테이지 클리어 조건은 없다. 최대한 오래 생존하여 더 높은 점수를 획득하는 것이 목표이다.

---

## 5. 플레이어와 장애물의 행동

### 플레이어: 광부

- 게임 시작 시 화면 아래쪽 땅 위의 정해진 위치에 배치된다.
- A/D 입력에 따라 좌우로 이동한다.
- 돌이 떨어지는 위치에서 벗어나 생존한다.
- 돌과 충돌하면 이동을 멈추고 게임이 종료된다.

### 장애물: 돌

- 게임이 시작되면 일정한 간격마다 돌 한 개를 생성한다.
- 이전 돌이 떨어지는 동안에도 다음 돌을 생성하여 여러 돌이 동시에 화면에 존재할 수 있다.
- 돌은 화면 위쪽 바깥의 무작위 가로 위치에서 생성된다.
- 생성 위치는 돌이 화면 안으로 들어올 수 있는 범위로 제한한다.
- 생성된 돌은 수직 아래 방향으로 일정한 속도로 이동한다.
- 땅에 닿은 돌은 제거한다.
- 같은 종류의 돌을 여러 개 생성하여 사용한다.

### 충돌 처리

광부와 돌의 사각형 충돌 영역이 닿거나 겹치면 충돌로 판정한다.

한 번 충돌하면 목숨을 잃고 게임이 종료된다. 종료 시에는 광부 이동, 돌 생성과 낙하, 생존 시간과 점수 증가를 모두 멈춘다.

돌이 땅에 닿으면 해당 돌을 제거한다.

---

## 6. 점수 규칙

생존 점수는 게임이 진행되는 동안 완전히 지난 1초마다 10점씩 증가한다.

```text
점수 = 생존 시간의 정수 부분 × 10
```

| 생존 시간 | 점수 |
|---|---:|
| 5초 | 50점 |
| 12.8초 | 120점 |
| 30초 | 300점 |
| 60초 | 600점 |

- 타이틀 화면과 결과 화면에서는 점수가 증가하지 않는다.
- 돌을 피한 횟수에 따른 별도 점수는 없다.
- 광부가 돌과 충돌한 순간의 점수를 최종 점수로 확정한다.
- 새로운 게임에서는 생존 시간과 점수를 처음부터 계산한다.

---

## 7. 게임 시작·종료·재시작 조건

### 게임 시작

- 프로그램을 실행하면 타이틀 화면을 표시한다.
- 타이틀 화면에는 게임 시작과 게임 종료 항목을 배치한다.
- 처음에는 게임 시작 항목을 선택한 상태로 표시한다.
- W/S로 항목을 선택하고 Space 키로 실행한다.
- 게임 시작 항목을 실행하면 플레이 화면으로 전환하여 게임을 시작한다.

### 게임 종료

- 광부와 돌이 충돌하면 진행 중인 게임을 종료한다.
- 게임 진행을 멈추고 최종 점수와 생존 시간을 확정한다.
- 결과 화면으로 전환하여 확정된 기록을 표시한다.

### 다시 시작

- 결과 화면에는 다시 시작과 게임 종료 항목을 배치한다.
- 처음에는 다시 시작 항목을 선택한 상태로 표시한다.
- W/S로 항목을 선택하고 Space 키로 실행한다.
- 다시 시작 항목을 실행하면 새로운 게임을 시작한다.

### 프로그램 종료

타이틀 화면 또는 결과 화면에서 게임 종료 항목을 선택하고 Space 키로 실행하면 프로그램을 종료한다.

---

## 8. 게임 진행 흐름

```text
프로그램 실행
      ↓
타이틀 화면
      ├─ 게임 종료 선택 → Space → 프로그램 종료
      │
      └─ 게임 시작 선택 → Space
                  ↓
              게임 플레이
              ├─ 광부 이동
              ├─ 돌 생성과 낙하
              ├─ 생존 시간과 점수 증가
              └─ 돌과 광부·땅의 충돌 검사
                  ├─ 땅과 충돌 → 해당 돌 제거 후 플레이 계속
                  │
                  └─ 광부와 충돌
                          ↓
                       결과 화면
                          ├─ 다시 시작 선택 → Space → 게임 플레이
                          │
                          └─ 게임 종료 선택 → Space → 프로그램 종료
```

---

## 9. 게임 화면 구성

화면은 세로형으로 구성한다. 타이틀, 플레이, 결과 화면에서 광산 배경 이미지 한 장을 공통으로 사용한다.

### 타이틀 화면

```text
┌────────────────────────┐
│                        │
│       StoneRush        │
│                        │
│                        │
│    ▶ [게임 시작]       │
│                        │
│      [게임 종료]       │
│                        │
│                        │
│      W / S : 선택      │
│     Space : 상호작용   │
│                        │
└────────────────────────┘
```

| 화면 요소 | 내용 |
|---|---|
| 배경 | 광산 배경 표시 |
| 게임 제목 | StoneRush 표시 |
| 게임 시작 | 선택 후 Space 키로 플레이 시작 |
| 게임 종료 | 선택 후 Space 키로 프로그램 종료 |
| 선택 표시 | 현재 선택한 버튼을 확대하여 표시 |
| 조작 안내 | UI 이동과 상호작용 방법 표시 |

### 플레이 화면

```text
┌────────────────────────┐
│ 점수: 120              │
│ 생존 시간: 12초        │
│                        │
│       O                │
│       ↓          O     │
│                  ↓     │
│                        │
│            O           │
│            ↓           │
│                        │
│    ← [광부] →          │
│────────────────────────│
│           땅           │
└────────────────────────┘
```

`O`와 화살표는 돌의 위치와 이동 방향을 설명하는 표시이다. 실제 게임에서는 돌 이미지를 사용한다.

| 화면 요소 | 내용 |
|---|---|
| 상단 정보 | 현재 점수와 생존 시간 표시 |
| 화면 위쪽과 중앙 | 생성되어 떨어지는 돌 표시 |
| 화면 아래쪽 | 좌우로 이동하는 광부 표시 |
| 땅 | 광부가 서 있는 지면이며, 돌이 닿으면 제거되는 위치 |
| 배경 | 광산 내부와 땅을 표현 |

### 결과 화면

```text
┌────────────────────────┐
│                        │
│       GAME OVER        │
│                        │
│    생존 시간: 30초     │
│    최종 점수: 300점    │
│                        │
│    ▶ [다시 시작]       │
│                        │
│      [게임 종료]       │
│                        │
│      W / S : 선택      │
│     Space : 상호작용   │
│                        │
└────────────────────────┘
```

| 화면 요소 | 내용 |
|---|---|
| 종료 문구 | GAME OVER 표시 |
| 결과 정보 | 생존 시간과 최종 점수 표시 |
| 다시 시작 | 선택 후 Space 키로 새로운 게임 시작 |
| 게임 종료 | 선택 후 Space 키로 프로그램 종료 |
| 선택 표시 | 현재 선택한 버튼을 확대하여 표시 |
| 조작 안내 | UI 이동과 상호작용 방법 표시 |

화면 구성도의 `▶`는 선택 상태를 설명하기 위한 표시이며, 실제 게임에서는 선택한 버튼을 확대한다.

---

## 10. 주요 게임 객체 목록

| 객체 | 역할 |
|---|---|
| 광부 | 좌우로 이동하며 돌을 피하는 플레이어 캐릭터이다. |
| 돌 | 화면 위에서 생성되어 아래로 떨어지는 장애물이다. |
| 땅 | 광부가 서 있는 지면이며, 돌과 충돌하면 해당 돌을 제거하는 기준이 된다. |
| 광산 배경 | 게임의 공간과 광산 내부를 표현한다. |
| 점수·시간 UI | 현재 점수와 생존 시간을 표시한다. |
| 메뉴 버튼 | 게임 시작, 다시 시작, 프로그램 종료를 실행한다. |

---

## 11. 필요한 이미지 리소스 목록

| 이미지 | 수량 | 용도 |
|---|---:|---|
| 광부 이미지 | 1종 | 플레이어 캐릭터 표시 |
| 돌 이미지 | 1종 | 모든 낙하 장애물에 공통 사용 |
| 광산 배경 이미지 | 1장 | 타이틀·플레이·결과 화면에 공통 사용 |
| 게임 시작 버튼 | 1개 | 타이틀 화면에서 게임 시작 |
| 게임 종료 버튼 | 1개 | 타이틀·결과 화면에 공통 사용 |
| 다시 시작 버튼 | 1개 | 결과 화면에서 새로운 게임 시작 |

땅은 광산 배경 이미지에 포함하여 표현한다.

게임 제목, 점수, 생존 시간, GAME OVER와 조작 안내는 텍스트로 출력한다.

버튼의 선택 상태는 기존 버튼 이미지를 확대하여 표현한다.

---

## 12. 필요한 사운드 리소스 목록

| 사운드 | 수량 | 용도 |
|---|---:|---|
| 플레이 BGM | 1개 | 게임 플레이 중 반복 재생 |
| 충돌 효과음 | 1개 | 광부와 돌이 충돌했을 때 재생 |
| 버튼 클릭 효과음 | 1개 | Space 키로 버튼을 실행할 때 딸깍 소리 재생 |

게임을 시작하면 플레이 BGM을 재생한다. 광부와 돌이 충돌하여 게임이 종료되면 BGM을 멈추고 충돌 효과음을 한 번 재생한다.

다시 시작하면 플레이 BGM을 다시 재생한다.

버튼 클릭 효과음은 게임 시작, 다시 시작, 게임 종료 버튼에 공통으로 사용한다.

---

## 13. 제작 범위 및 구현 순서

제작 범위는 광부의 좌우 이동, 돌 생성과 낙하, 광부·땅과 돌의 충돌 판정, 생존 점수, 타이틀·플레이·결과 화면, 재시작과 프로그램 종료, 기본 이미지와 사운드 적용으로 정한다.

광부 1종, 돌 1종, 땅이 포함된 광산 배경 1장을 사용하여 게임의 시작부터 종료와 재시작까지 정상적으로 동작하는 것을 목표로 한다.

| 단계 | 구현 내용 |
|---|---|
| 1단계 | 광부 출력, 좌우 이동과 화면 경계 처리 |
| 2단계 | 돌 생성과 낙하 |
| 3단계 | 광부·땅과 돌의 충돌 판정 및 땅에 닿은 돌 제거 |
| 4단계 | 광부와 돌의 충돌 시 게임 종료와 결과 화면 전환 |
| 5단계 | 생존 점수, 타이틀 메뉴와 재시작 |
| 6단계 | 이미지·사운드 적용과 전체 플레이 확인 |